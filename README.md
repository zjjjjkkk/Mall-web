# Mall Admin Web - 电商后台管理系统 v2.0

## 项目概述

`mall-admin-web`是一个基于Vue.js和Element UI开发的电商后台管理系统前端项目，提供了完整的电商后台管理功能。该项目与后端服务（`mall-admin`）配合，实现了商品管理、订单管理、会员管理、促销管理、运营管理、内容管理、统计报表等核心功能，为电商平台提供了高效、易用的管理界面。

## 项目简介
管理端为运营、客服、内容、仓储、财务提供一站式后台，覆盖商品/订单/退款/报修/促销/会员/UGC/统计等全链路，配合后端 `mall-admin` 与搜索子系统。

## 技术栈分析（选型与缘由）
- Vue2 + Vue Router + Vuex：成熟稳定、生态完善，便于权限路由与状态集中管理。
- Element UI：后台场景友好的组件库，提升表单/表格/弹窗开发效率。
- Axios 封装：统一请求、鉴权、错误处理；nprogress 提示加载态。
- ECharts / v-charts：数据大屏、统计报表可视化。
- TinyMCE：富文本编辑（内容/公告）。
- Webpack+Babel：成熟构建链路，支持代码分割与按需加载。

## 整体设计分析
- 物理结构：`src/api` 接口、`views` 业务页面、`components` 通用组件、`router` 权限路由、`store` 全局状态、`utils` 工具、`styles` 样式与主题。
- 逻辑结构：视图层（views）→ 路由守卫（permission.js）→ API 封装（request.js）→ 后端服务；Vuex 管理用户/权限/应用态。
- 模块划分：PMS 商品、OMS 订单/退款、UMS 会员、SMS 促销、内容/UGC 管理、报修管理、统计大屏、权限管理。

## 代码实现要点（本版新增与关键）
- UGC 审核与管理：帖子/评论列表、审核、置顶/删除；对应后端 `ugc` 接口。
- 搜索与统计：配合 ES 搜索结果、行为/订单/销售统计；数据大屏用 ECharts。
- 报修管理增强：支持录入物流公司与单号、状态全流程。
- 退款管理：退款列表、审核、统计。
- 体验优化：Axios 数组参数序列化修复、表格分页查询性能优化、统一代码风格。

## 📋 版本更新日志

### v2.0.0 (2025-12-09) - 重大功能更新

#### 🎉 核心新增功能

**1. UGC内容管理系统**
- ✅ 帖子管理：帖子列表查询、审核、置顶、删除
- ✅ 评论管理：评论列表、评论审核（显示/隐藏）、批量删除
- ✅ 点赞管理：查看帖子点赞列表
- ✅ 内容审核：支持帖子/评论的审核功能

**2. 数据统计大屏**
- ✅ 订单趋势图表：按天/按小时查看订单趋势
- ✅ 商品销售排行：横向柱状图展示销量排行
- ✅ 地区销售分布：饼图展示地区销售额占比
- ✅ 自定义查询：支持自定义日期范围

**3. 用户行为统计**
- ✅ 行为类型统计：按行为类型统计（浏览、搜索、点击、购买）
- ✅ 商品行为统计：统计商品相关的用户行为
- ✅ 行为趋势统计：按时间趋势统计用户行为

**4. 商品报修管理增强**
- ✅ 发货管理：待发货订单发货功能
- ✅ 物流管理：录入物流公司和物流单号
- ✅ 状态管理：完整的报修状态流转

**5. 退款管理**
- ✅ 退款订单列表：退款订单查询、筛选
- ✅ 退款审核：同意/拒绝退款申请
- ✅ 退款统计：退款数据统计

**6. 积分与成长值管理（新增）**
- ✅ 积分流水查询：查看用户积分变动记录
- ✅ 成长值历史查询：查看用户成长值变动和等级变化
- ✅ 会员权益管理：查看用户会员类型和有效期

**7. 商品对比功能（新增）**
- ✅ 对比列表管理：查看用户商品对比列表
- ✅ 对比数据统计：统计对比功能使用情况

#### 🔧 技术优化

- ✅ 数组参数处理：修复axios数组参数序列化问题
- ✅ 表格优化：优化表格查询和分页性能
- ✅ 图表优化：使用ECharts实现数据可视化
- ✅ 代码规范：统一代码风格和命名规范

---

### v1.0.0 - 基础版本

包含基础管理功能：
- 商品管理、订单管理
- 会员管理、权限管理
- 促销管理、内容管理

## ✨ 核心特性

- 🎨 **现代化UI设计**：基于Element UI 2.x，界面美观、交互流畅
- 📱 **响应式布局**：支持不同屏幕尺寸，适配PC和移动端
- 🔐 **权限管理**：基于角色的权限控制（RBAC）
- 📊 **数据可视化**：集成ECharts图表库，支持数据统计展示
- 🚀 **性能优化**：代码分割、懒加载、缓存优化
- 📝 **富文本编辑**：集成TinyMCE富文本编辑器
- 📤 **文件上传**：支持图片、文件上传到OSS/MinIO

## 技术栈

| 技术框架/库        | 版本/说明                  | 官方文档                                                     |
|--------------------|---------------------------|------------------------------------------------------------|
| Vue                | 2.x (前端框架)            | [https://vuejs.org/](https://vuejs.org/)                     |
| Vue Router         | 3.x (路由管理)            | [https://router.vuejs.org/](https://router.vuejs.org/)       |
| Vuex               | 3.x (状态管理)            | [https://vuex.vuejs.org/](https://vuex.vuejs.org/)           |
| Element UI         | 2.x (UI组件库)            | [https://element.eleme.io/](https://element.eleme.io/)       |
| Axios              | HTTP请求库                | [https://github.com/axios/axios](https://github.com/axios/axios) |
| v-charts           | 基于Echarts的图表库        | [https://v-charts.js.org/](https://v-charts.js.org/)         |
| Js-cookie          | Cookie管理工具            | [https://github.com/js-cookie/js-cookie](https://github.com/js-cookie/js-cookie) |
| nprogress          | 页面加载进度条            | [https://github.com/rstacruz/nprogress](https://github.com/rstacruz/nprogress) |
| TinyMCE            | 富文本编辑器              | [https://www.tiny.cloud/](https://www.tiny.cloud/)           |
| Webpack            | 4.x (模块打包工具)        | [https://webpack.js.org/](https://webpack.js.org/)           |
| Babel              | ES6+转译工具              | [https://babeljs.io/](https://babeljs.io/)                   |

## 📁 项目结构

```
web/                      # 项目根目录
├── build/               # Webpack构建配置目录
│   ├── build.js        # 生产环境构建脚本
│   ├── check-versions.js # 版本检查脚本
│   ├── utils.js        # 构建工具函数
│   ├── vue-loader.conf.js # Vue加载器配置
│   ├── webpack.base.conf.js # Webpack基础配置
│   ├── webpack.dev.conf.js # Webpack开发环境配置
│   └── webpack.prod.conf.js # Webpack生产环境配置
├── config/             # 项目配置目录
│   ├── dev.env.js      # 开发环境变量配置
│   ├── index.js        # 项目主配置文件
│   └── prod.env.js     # 生产环境变量配置
├── src/                # 项目源码目录
│   ├── api/            # API接口定义目录
│   │   ├── brand.js          # 品牌管理接口
│   │   ├── companyAddress.js # 公司地址接口
│   │   ├── coupon.js         # 优惠券接口
│   │   ├── flash.js          # 秒杀活动接口
│   │   ├── order.js          # 订单管理接口
│   │   ├── product.js        # 商品管理接口
│   │   ├── returnApply.js    # 退货申请接口
│   │   ├── returnReason.js   # 退货原因接口
│   │   ├── refundOrder.js    # 退款订单接口
│   │   ├── repair.js         # 报修管理接口（含发货功能）
│   │   ├── statistics.js     # 数据统计接口（新增）
│   │   ├── ugc.js            # UGC帖子管理接口（含评论管理，新增）
│   │   └── ...               # 其他接口文件
│   ├── assets/         # 静态资源目录
│   │   └── images/     # 图片资源
│   ├── components/     # 通用组件目录
│   │   ├── Breadcrumb/ # 面包屑组件
│   │   ├── Hamburger/  # 汉堡菜单组件
│   │   ├── ScrollBar/  # 滚动条组件
│   │   ├── SvgIcon/    # SVG图标组件
│   │   ├── Tinymce/    # 富文本编辑器组件
│   │   └── Upload/     # 文件上传组件
│   ├── icons/          # SVG图标资源目录
│   │   ├── index.js    # 图标注册入口
│   │   └── svg/        # SVG图标文件
│   ├── router/         # 路由配置目录
│   │   └── index.js    # 路由配置文件（含权限控制）
│   ├── store/          # Vuex状态管理目录
│   │   ├── getters.js  # Getter定义
│   │   ├── index.js    # Store入口
│   │   └── modules/    # 模块化状态管理
│   │       ├── user.js      # 用户状态管理
│   │       ├── app.js       # 应用状态管理
│   │       └── permission.js # 权限状态管理
│   ├── styles/         # 样式文件目录
│   │   ├── element-ui.scss # Element UI样式覆盖
│   │   ├── index.scss   # 全局样式入口
│   │   ├── mixin.scss   # 样式混合
│   │   ├── sidebar.scss # 侧边栏样式
│   │   ├── transition.scss # 过渡动画样式
│   │   └── variables.scss # 样式变量
│   ├── utils/          # 工具函数目录
│   │   ├── auth.js     # 认证相关工具（Token管理）
│   │   ├── date.js     # 日期处理工具
│   │   ├── index.js    # 工具函数入口
│   │   ├── request.js  # Axios请求封装
│   │   ├── support.js  # 支持函数
│   │   └── validate.js # 表单验证函数
│   ├── views/          # 页面组件目录
│   │   ├── 404.vue     # 404页面
│   │   ├── home/       # 首页模块（数据统计）
│   │   ├── statistics/ # 数据统计模块（新增）
│   │   │   └── dashboard.vue  # 数据大屏页面
│   │   ├── ugc/        # UGC内容管理模块（新增）
│   │   │   └── post/    # 帖子管理
│   │   │       └── index.vue  # 帖子列表页面
│   │   ├── layout/     # 布局组件
│   │   │   ├── components/  # 布局子组件
│   │   │   │   ├── Sidebar/  # 侧边栏
│   │   │   │   ├── Navbar/   # 导航栏
│   │   │   │   └── TagsView/ # 标签页
│   │   │   └── index.vue     # 主布局
│   │   ├── login/      # 登录页面
│   │   ├── oms/        # 订单管理模块
│   │   │   ├── order/        # 订单管理
│   │   │   ├── returnApply/  # 退货申请
│   │   │   └── refundOrder/  # 退款订单（新增）
│   │   ├── pms/        # 商品管理模块
│   │   │   ├── product/      # 商品管理
│   │   │   ├── productCate/   # 商品分类
│   │   │   ├── brand/         # 品牌管理
│   │   │   ├── productAttr/   # 商品属性
│   │   │   └── repair/        # 商品报修管理（含发货功能）
│   │   ├── sms/        # 营销管理模块
│   │   │   ├── coupon/        # 优惠券管理
│   │   │   ├── flash/         # 秒杀活动
│   │   │   └── home/          # 首页推荐
│   │   └── ums/        # 用户管理模块
│   │       ├── admin/         # 管理员管理
│   │       ├── role/          # 角色管理
│   │       ├── menu/          # 菜单管理
│   │       └── resource/      # 资源管理
│   ├── App.vue         # 根组件
│   ├── main.js         # 应用入口文件
│   └── permission.js   # 权限管理文件（路由守卫）
├── static/             # 静态文件目录
│   └── tinymce4.7.5/   # TinyMCE富文本编辑器
├── index.html          # HTML入口文件
├── package.json        # 项目依赖配置
├── package-lock.json   # 依赖版本锁定文件
└── README.md           # 项目说明文档
```

## 🎯 功能模块详解

### 1. 订单管理系统 (OMS)

#### 订单管理
- **订单列表**：
  - 订单查询（支持订单号、收货人、手机号搜索）
  - 订单筛选（按状态、时间范围筛选）
  - 订单详情查看
  - 订单发货（录入物流单号）
  - 订单备注
  - 订单导出
- **订单详情**：
  - 订单基本信息
  - 商品信息列表
  - 收货地址信息
  - 支付信息
  - 物流信息
  - 订单操作日志

#### 退货管理
- **退货申请列表**：
  - 退货申请查询
  - 退货申请审核（同意/拒绝）
  - 退货原因管理
  - 退货进度跟踪

#### 退款管理（新增）
- **退款订单列表**：
  - 退款订单查询
  - 退款订单详情
  - 退款审核（同意/拒绝）
  - 退款状态跟踪
  - 退款统计

#### 订单设置
- 订单超时时间配置
- 自动取消规则设置

### 2. 商品管理系统 (PMS)

#### 商品分类管理
- 分类树形结构展示
- 分类添加、编辑、删除
- 分类排序
- 分类图标上传

#### 商品信息管理
- **商品列表**：
  - 商品查询、筛选
  - 商品上下架
  - 商品删除
- **商品添加/编辑**：
  - 基本信息（名称、价格、库存等）
  - 商品图片（主图、详情图）
  - 商品规格（SKU管理）
  - 商品属性
  - 商品详情（富文本编辑）
  - 商品促销设置

#### 品牌管理
- 品牌列表
- 品牌添加、编辑、删除
- 品牌Logo上传
- 品牌排序

#### 商品属性管理
- 属性分类管理
- 商品属性管理
- 属性值管理

#### 库存管理
- SKU库存查询
- 库存调整
- 库存预警

#### 商品报修管理（新增）
- **报修列表**：
  - 报修申请查询（支持按品牌、状态筛选）
  - 报修详情查看
  - 报修状态更新（待处理、已受理、维修中、已完成、已取消、待支付、待发货、已发货）
  - 报修进度跟踪
- **报修信息**：
  - 报修商品信息
  - 报修用户信息
  - 报修方式（邮寄/线下到店）
  - 报修描述和图片
  - 位置信息（线下到店时显示距离、时间）
  - 支付信息（支付金额、支付状态、支付时间）
  - 收货信息（收货人、电话、地址）
  - 物流信息（物流公司、物流单号、发货时间）
- **发货管理**（新增）：
  - 待发货订单发货功能
  - 录入物流公司和物流单号
  - 自动更新报修状态为"已发货"

### 3. 营销管理系统 (SMS)

#### 优惠券管理
- **优惠券列表**：
  - 优惠券查询
  - 优惠券添加、编辑、删除
  - 优惠券类型（全场券、分类券、商品券）
  - 优惠券发放数量设置
  - 优惠券使用统计
- **优惠券历史**：
  - 优惠券领取记录
  - 优惠券使用记录

#### 秒杀活动管理
- **秒杀场次管理**：
  - 场次添加、编辑
  - 场次时间设置
- **秒杀商品管理**：
  - 秒杀商品添加
  - 秒杀价格设置
  - 秒杀库存设置

#### 首页推荐管理
- **轮播图管理**：
  - 轮播图添加、编辑、删除
  - 轮播图排序
  - 轮播图链接设置
- **推荐品牌管理**：
  - 推荐品牌设置
  - 品牌排序
- **推荐商品管理**：
  - 推荐商品设置
  - 商品排序
- **新品推荐管理**：
  - 新品推荐设置

### 4. 用户管理系统 (UMS)

#### 会员管理
- 会员列表查询
- 会员详情查看
- 会员信息编辑
- 会员积分管理
- 会员等级管理

#### 权限管理
- **管理员管理**：
  - 管理员列表
  - 管理员添加、编辑、删除
  - 管理员状态管理
- **角色管理**：
  - 角色列表
  - 角色添加、编辑、删除
  - 角色权限分配
- **菜单管理**：
  - 菜单树形结构
  - 菜单添加、编辑、删除
  - 菜单权限配置
- **资源管理**：
  - 资源列表
  - 资源分类管理
  - 资源权限配置

### 5. UGC内容管理系统（新增）

#### 帖子管理
- **帖子列表**：
  - 帖子查询（支持关键词、状态、置顶筛选）
  - 帖子详情查看
  - 帖子审核（发布/下架）
  - 帖子置顶管理
  - 帖子删除（批量删除）
- **帖子信息**：
  - 帖子标题、内容
  - 封面图、媒体URL（图片/视频）
  - 媒体类型（文本、图片、视频、混合）
  - 发帖用户信息
  - 点赞数、浏览数、评论数
  - 发布时间、更新时间

#### 评论管理（新增）
- **评论列表**：
  - 评论查询（按帖子ID查询）
  - 评论详情查看
  - 评论审核（显示/隐藏）
  - 评论删除（单个/批量删除）
- **评论信息**：
  - 评论内容、用户信息
  - 点赞数、回复数
  - 评论时间
  - 审核状态（显示/隐藏）

### 6. 数据统计系统（新增）

#### 数据大屏
- **订单趋势图表**：
  - 支持按天/按小时查看订单趋势
  - 显示订单数、订单金额、已支付订单数、已支付金额
  - 支持自定义日期范围查询
  - 使用ECharts实现数据可视化
- **商品销售排行**：
  - 横向柱状图展示商品销量排行
  - 支持自定义日期范围查询
  - 显示商品名称、销量、销售额
- **地区销售分布**：
  - 饼图展示各地区销售额占比
  - 支持自定义日期范围查询
  - 显示省份、城市、订单数、销售额
- **数据刷新**：
  - 支持手动刷新所有图表数据
  - 刷新按钮显示加载状态

#### 用户行为统计（新增）
- **行为类型统计**：
  - 按行为类型统计（浏览、搜索、点击、购买等）
  - 支持自定义日期范围查询
  - 显示行为类型、行为次数、占比
- **商品行为统计**：
  - 统计商品相关的用户行为
  - 显示商品名称、行为类型、行为次数
  - 支持按行为类型筛选
- **行为趋势统计**：
  - 按时间趋势统计用户行为
  - 支持按天/按小时查看
  - 显示行为趋势图表

## 🚀 快速开始

### 环境要求
- Node.js >= 8.9.0
- npm >= 5.6.0
- 或 yarn >= 1.0.0

### 安装依赖

```bash
# 使用npm
npm install

# 或使用yarn
yarn install
```

### 开发环境运行

```bash
# 启动开发服务器
npm run dev
```

项目将在 `http://localhost:8092` 启动

**开发环境配置**：
- 后端API地址：`http://localhost:8080`
- 开发服务器端口：`8092`
- 支持热更新（HMR）

**新增功能路由**：
- `/ugc/post` - UGC帖子管理页面（新增）

### 生产环境构建

```bash
# 构建生产环境
npm run build

# 构建并查看构建报告（分析打包体积）
npm run build --report
```

构建产物将输出到 `dist` 目录

### 环境变量配置

**开发环境** (`config/dev.env.js`)
```javascript
module.exports = {
  NODE_ENV: '"development"',
  BASE_API: '"http://localhost:8080"'  // 后端API地址
}
```

**生产环境** (`config/prod.env.js`)
```javascript
module.exports = {
  NODE_ENV: '"production"',
  BASE_API: '"https://api.yourdomain.com"'  // 生产环境API地址
}
```

## 📖 开发指南

### 代码规范
- 遵循Vue.js官方代码风格指南
- 使用ES6+语法
- 组件命名采用大驼峰式命名（PascalCase）
- API接口函数命名采用动词+名词形式（如：`fetchList`、`createBrand`）
- 变量命名采用驼峰式命名（camelCase）

### 目录结构规范
- **页面组件**：放在 `src/views/` 目录下，按模块分类
- **通用组件**：放在 `src/components/` 目录下
- **API接口定义**：放在 `src/api/` 目录下，按模块分类
- **工具函数**：放在 `src/utils/` 目录下
- **样式文件**：放在 `src/styles/` 目录下

### 接口调用

```javascript
// 1. 在 api/ 目录下定义接口
// api/brand.js
import request from '@/utils/request'

export function fetchList(params) {
  return request({
    url: '/brand/list',
    method: 'get',
    params: params
  })
}

export function createBrand(data) {
  return request({
    url: '/brand/create',
    method: 'post',
    data: data
  })
}

// 2. 在组件中使用
import { fetchList, createBrand } from '@/api/brand'

export default {
  data() {
    return {
      list: []
    }
  },
  mounted() {
    this.loadData()
  },
  methods: {
    async loadData() {
      const response = await fetchList({ pageNum: 1, pageSize: 10 })
      this.list = response.data.list
    },
    async handleCreate(data) {
      await createBrand(data)
      this.$message.success('创建成功')
      this.loadData()
    }
  }
}
```

### 状态管理（Vuex）

```javascript
// store/modules/user.js
const state = {
  token: getToken(),
  name: '',
  avatar: ''
}

const mutations = {
  SET_TOKEN: (state, token) => {
    state.token = token
  },
  SET_NAME: (state, name) => {
    state.name = name
  }
}

// 在组件中使用
import { mapState, mapMutations } from 'vuex'

export default {
  computed: {
    ...mapState('user', ['token', 'name'])
  },
  methods: {
    ...mapMutations('user', ['SET_TOKEN']),
    handleLogin() {
      this.SET_TOKEN('new-token')
    }
  }
}
```

### 路由配置

```javascript
// router/index.js
{
  path: '/product',
  component: Layout,
  redirect: '/product/list',
  name: 'Product',
  meta: { title: '商品管理', icon: 'product' },
  children: [
    {
      path: 'list',
      name: 'ProductList',
      component: () => import('@/views/pms/product/index'),
      meta: { title: '商品列表', icon: 'product-list' }
    }
  ]
}
```

### 权限控制

系统使用路由守卫实现权限控制：

```javascript
// permission.js
router.beforeEach((to, from, next) => {
  // 检查token
  if (store.getters.token) {
    // 已登录，检查权限
    if (hasPermission(to.meta.roles)) {
      next()
    } else {
      next({ path: '/401' })
    }
  } else {
    // 未登录，跳转登录页
    if (whiteList.includes(to.path)) {
      next()
    } else {
      next('/login')
    }
  }
})
```

## 🎨 UI组件使用

### Element UI组件

```vue
<template>
  <el-table :data="tableData" style="width: 100%">
    <el-table-column prop="name" label="名称" width="180"></el-table-column>
    <el-table-column prop="price" label="价格" width="180"></el-table-column>
    <el-table-column label="操作">
      <template slot-scope="scope">
        <el-button size="mini" @click="handleEdit(scope.row)">编辑</el-button>
        <el-button size="mini" type="danger" @click="handleDelete(scope.row)">删除</el-button>
      </template>
    </el-table-column>
  </el-table>
</template>
```

### 自定义组件

**文件上传组件**：
```vue
<template>
  <upload
    :action="uploadUrl"
    :file-list="fileList"
    @on-success="handleUploadSuccess"
  />
</template>
```

**富文本编辑器**：
```vue
<template>
  <tinymce
    v-model="content"
    :height="400"
  />
</template>
```

## 🔧 常用功能实现

### 1. 表格查询和分页

```vue
<template>
  <div>
    <!-- 搜索表单 -->
    <el-form :inline="true" :model="searchForm">
      <el-form-item label="订单号">
        <el-input v-model="searchForm.orderSn" placeholder="请输入订单号"></el-input>
      </el-form-item>
      <el-form-item>
        <el-button type="primary" @click="handleSearch">查询</el-button>
        <el-button @click="handleReset">重置</el-button>
      </el-form-item>
    </el-form>

    <!-- 数据表格 -->
    <el-table :data="list" v-loading="loading">
      <!-- 表格列 -->
    </el-table>

    <!-- 分页 -->
    <pagination
      v-show="total > 0"
      :total="total"
      :page.sync="listQuery.pageNum"
      :limit.sync="listQuery.pageSize"
      @pagination="getList"
    />
  </div>
</template>
```

### 2. 表单提交

```vue
<template>
  <el-form ref="form" :model="form" :rules="rules" label-width="100px">
    <el-form-item label="品牌名称" prop="name">
      <el-input v-model="form.name"></el-input>
    </el-form-item>
    <el-form-item>
      <el-button type="primary" @click="submitForm">提交</el-button>
      <el-button @click="resetForm">重置</el-button>
    </el-form-item>
  </el-form>
</template>

<script>
export default {
  data() {
    return {
      form: {
        name: ''
      },
      rules: {
        name: [
          { required: true, message: '请输入品牌名称', trigger: 'blur' }
        ]
      }
    }
  },
  methods: {
    submitForm() {
      this.$refs.form.validate((valid) => {
        if (valid) {
          // 提交表单
          this.createBrand(this.form)
        }
      })
    }
  }
}
</script>
```

### 3. 图片上传

```vue
<template>
  <upload
    :action="uploadAction"
    :file-list="fileList"
    :limit="1"
    @on-success="handleUploadSuccess"
  />
</template>

<script>
export default {
  data() {
    return {
      uploadAction: process.env.BASE_API + '/admin/upload/oss',
      fileList: []
    }
  },
  methods: {
    handleUploadSuccess(response, file) {
      this.form.pic = response.data
    }
  }
}
</script>
```

## 🌐 浏览器支持

- Chrome (推荐)
- Firefox
- Safari
- Internet Explorer 11+
- Edge

## 📦 构建部署

### 开发环境
```bash
npm run dev
```

### 生产环境构建
```bash
npm run build
```

构建后的文件在 `dist` 目录，可以部署到Nginx、Apache等Web服务器。

### Nginx配置示例

```nginx
server {
    listen 80;
    server_name admin.yourdomain.com;
    root /var/www/mall-admin-web/dist;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
    }

    location /api {
        proxy_pass http://localhost:8080;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
}
```

## 🐛 常见问题

### 1. 接口请求失败
- 检查后端服务是否启动
- 检查 `config/dev.env.js` 中的 `BASE_API` 配置
- 检查浏览器控制台的网络请求

### 2. 登录后跳转404
- 检查路由配置是否正确
- 检查权限配置
- 查看浏览器控制台错误信息

### 3. 构建失败
- 检查Node.js版本是否符合要求
- 清除 `node_modules` 重新安装：`rm -rf node_modules && npm install`
- 检查是否有语法错误

## 📄 许可证

[MIT](https://opensource.org/licenses/MIT)

## 🤝 贡献指南

1. Fork 本仓库
2. 创建特性分支 (`git checkout -b feature/AmazingFeature`)
3. 提交更改 (`git commit -m 'Add some AmazingFeature'`)
4. 推送到分支 (`git push origin feature/AmazingFeature`)
5. 开启 Pull Request

## 📞 联系方式

如有问题或建议，欢迎提交 Issue 或 Pull Request。

---

**感谢使用 Mall Admin Web！** 🎉
